services:
  - type: web
    name: nanobot
    runtime: docker
    # Using Docker runtime is recommended for the best experience.
    # The gateway automatically respects the $PORT environment variable.
    startCommand: ./render-start.sh
    envVars:
      - key: NANOBOT_CONFIG
        sync: false
      - key: GROQ_API_KEY
        sync: false
