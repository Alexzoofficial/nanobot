services:
  - type: web
    name: nanobot
    runtime: docker
    # The Dockerfile handles build and start
    # The gateway automatically respects the $PORT environment variable
    # and can be configured via NANOBOT_CONFIG or NANOBOT__* env vars.
    envVars:
      - key: NANOBOT_CONFIG
        sync: false
